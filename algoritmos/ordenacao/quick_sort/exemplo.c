#include <stdio.h>
#include "quick_sort.h"

int main() {
    printf("Executando exemplo para quick_sort...\n");
    int arr[] = {5, 2, 4, 6, 1, 3};
    int n = sizeof(arr)/sizeof(arr[0]);

    // Chamar a função principal do módulo
    // quick_sort_sort(arr, n);

    printf("Exemplo concluído.\n");
    return 0;
}
