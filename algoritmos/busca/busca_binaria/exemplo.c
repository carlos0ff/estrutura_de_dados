#include <stdio.h>
#include "busca_binaria.h"

int main() {
    printf("Executando exemplo para busca_binaria...\n");
    int arr[] = {5, 2, 4, 6, 1, 3};
    int n = sizeof(arr)/sizeof(arr[0]);

    // Chamar a função principal do módulo
    // busca_binaria_sort(arr, n);

    printf("Exemplo concluído.\n");
    return 0;
}
